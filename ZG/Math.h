#pragma once

#include "Core.h"

#define ZG_MIN(x, y) ((x) < (y) ? (x) : (y))
#define ZG_MAX(x, y) ((x) > (y) ? (x) : (y))

#define ZG_ABS(x, y) ((x) > (y) ? (x) - (y) : (y) - (x))

#ifdef __cplusplus
extern "C" {
#endif
#ifdef __cplusplus
}
#endif